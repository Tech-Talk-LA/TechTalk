import React from 'react';
import {Text, View, StatusBar, Alert} from 'react-native';
import styles from '../../assets/syles.js';

export default function MessageModal({ navigation }) {
  const messageBoxes = //map array of conversations to individual message box in message modal
  /* Each message box will show recipient's picture and name and button to navigate to conversation */
  return (
    <Messages>
      {messageBoxes}
    </Messages>
  )
}